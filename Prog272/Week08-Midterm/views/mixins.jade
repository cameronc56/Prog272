//
   Created by charlie on 2/17/15.

mixin attribution()
    p by Cameron Cooks
mixin bio() 
    p I like to play cello

mixin bootswatch(theme)
    link(href="//maxcdn.bootstrapcdn.com/bootswatch/3.3.0/" + theme + "/bootstrap.min.css", rel="stylesheet")

//- Navbar mixins
mixin nav(name, id, style)
    - var style = (typeof style === 'undefined') ? "navbar-default" : style
    nav( role="navigation", class=["navbar", style] )
        .container-fluid
            .navbar-header
                .navbar-header
                button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
                    span.sr-only Toggle navigation
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.navbar-brand(href='/') Cameron's Project
            .collapse.navbar-collapse( id=id )
                ul.nav.navbar-nav
                    block
mixin nav_item(href, idName, active)
    li(class=active, id=idName): a( href=href )
        block

mixin nav_item_dropdown(href, active)
    li.dropdown
        a.dropdown-toggle(href=href, data-toggle='dropdown', role='button', aria-expanded='false')
            | Dropdown
            span.caret
        ul.dropdown-menu(role='menu')
            block

mixin nav_item_dropdowna(href, active)
    li(class=["dropdown", active])
        a.dropdown-toggle( href=href, data-toggle="dropdown", role="button", aria-expanded="false" )= attributes.label
        span.caret
        ul.dropdown-menu( role="menu" )
            block
mixin nav_divider
    li.divider
mixin nav_header
    li.dropdown-header
        block
//- End navbar mixins

//- Panel mixin
mixin panel(heading, style)
    - var style = (typeof style === 'undefined') ? "default" : style
    div( class=["panel", "panel-" + style] )
        .panel-heading
          .panel-title=heading
        .panel-body
            block


//- Button mixin
mixin button(color, href, size, id)
    - var color = (typeof color === 'undefined') ? "default" : color
    - var href = (typeof href === 'undefined') ? "#" : href
    case size
        when "large"
            - size = "btn-lg"
        when "small"
            - size = "btn-sm"
        when "mini"
            - size = "btn-xs"
    a( class=["btn", "btn-" + color, size], style="margin-right: 10px; margin-bottom: 10px;", id=id)
        block

//- List group mixins
- var groupType
mixin listGroup(type, id)
    - groupType = type
    case groupType
        when 'list'
            ul.list-group(id = id)
                block
        default
            .list-group(id = id)
                block
mixin listItem(arg1, arg2)
    case groupType
        when 'list'
            li( class=["list-group-item", arg1] )
                block
        when 'links'
            a( href=arg1, class=["list-group-item", arg2] )
                block
        default
            .list-group-item( class=["list-group-item", arg1] )
                block
mixin listHeading
    h4.list-group-item-heading
        block
mixin listText
    .list-group-item-text
        block

//jumbotron mixins
mixin jumbotron
    div.jumbotron(style="margin-top: 10px")
        block
mixin container
    div.container
        block
//inputs
mixin input(placeholder, id)
    input(id=id, type="text", class="form-control", placeholder=placeholder, style="width: 200px; margin-bottom: 10px")
mixin radio()